{
  "name": "api-rate-limiter",
  "version": "1.0.0",
  "description": "Intelligent rate limiting for API calls with queueing and exponential backoff",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "bun test"
  },
  "keywords": [
    "rate-limiter",
    "api",
    "rate-limit",
    "backoff",
    "queue"
  ],
  "author": "The BookMaster",
  "license": "MIT",
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.0.0"
  }
}
